// TODO import { getConnection } from "typeorm";
//
// async function addProductFeedback(productId: number, rate: number, content: string) {
//     await getConnection().query(
//         `CALL add_feedback($1, $2, $3)`,
//         [productId, rate, content]
//     );
// }
